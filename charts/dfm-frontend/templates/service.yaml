apiVersion: v1
kind: Service
metadata:
  name: {{ include "dfm-frontend.fullname" . }}
  labels:
    {{- include "dfm-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dfm-frontend.selectorLabels" . | nindent 4 }}
